{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/contracts/*",
    "packages/modules/*",
    "packages/platform/*",
    "packages/lib/*"
  ],
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter web build"
  }
}
